{% extends 'base.html.twig' %}

{% block title %}
	{{infoMovies.title}}
{% endblock %}

{% block body %}
	<h1 class="titleInfo">{{infoMovies.title}}</h1>

	<div class="cardInfo">
		<div class="imgDateRuntime">
			<img src="https://www.themoviedb.org/t/p/w220_and_h330_face/{{infoMovies.poster_path}}" class="cardImgInfo" alt="placeholder">
			<h5>Date de sortie :
				{{infoMovies.release_date}}</h5>
			<h5>Durée :
				{{infoMovies.runtime}}min</h5>
		</div>
		<div class="card-body">
			<p class="cardTextInfo">{{infoMovies.overview|raw}}</p>
			<p class="card-text">Note moyenne: {{ infoMovies.vote_average }}</p>
		</div>
	</div>


{% endblock %}
